[build]
builder = "nixpacks"

[deploy]
startCommand = "CLAUDE_API_KEY=${{CLAUDE_API_KEY}} FEELING_PASSWORD=${{FEELING_PASSWORD}} python server.py"
healthcheckPath = "/healthz"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
